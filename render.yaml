services:
  - type: web
    name: your-service-name  # Replace with your service name
    env: node
    plan: free
    region: frankfurt  # Set to Frankfurt
    buildCommand: npm install
    startCommand: node server.js  # Use node server.js to start your server
    envVars:  # Correctly formatted environment variables
      - key: NODE_ENV
        value: production
