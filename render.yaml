services:
  - type: web
    name: your-service-name  # Replace with your desired service name
    env: node
    plan: free
    region: frankfurt  # Choose the Frankfurt region
    buildCommand: npm install
    startCommand: node server.js  # Command to start your server
    envVars:
      - key: NODE_ENV
        value: production
